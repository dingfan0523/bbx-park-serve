**邮件主题建议**：【整改回复】10.33.15.64 (CNV-BBXDATA-DEV) 安全扫描整改闭环说明 — SEC-REPORT-2026-001

**收件人**：（填安全扫描平台/安全团队）

---

各位：

针对目标主机 **10.33.15.64 (CNV-BBXDATA-DEV)** 的安全扫描结果（报告编号 SEC-REPORT-2026-001，报告日期 2026-07-16），本期共处置扫描检出项 **63 项**：其中 **57 项已修复并闭环**，**6 项经澄清后风险接受**。已修复项（Nginx / Tomcat / Java 应用依赖 / Kylin V10 系统软件包）均已通过版本升级或配置加固完成处理，可直接在扫描平台标记为【已修复】闭环。

现将未修复项的澄清说明与整体结论回复如下。

---

## 一、已澄清漏洞（未修复）

> **本章为本次整改回复的重点。** 以下漏洞经扫描工具检出并列为风险项，但经评估确认无需（或不可行）通过版本升级进行修复，本期统一采取**风险接受**策略，并逐条给出扫描闭环处置建议。其中 Spring / Shiro 相关项经代码审查确认在当前调用方式下不满足触发条件，且可行的升级路径成本与风险过高（详见下方补充原因）；pip 相关项因需本地权限、无常驻服务，实际攻击面有限（详见下文 Python/pip 部分）。

### 补充原因（CUD 框架澄清）

> **澄清范围**：CUD 框架及基于 CUD 框架开发的业务系统，扫描出以下 5 类漏洞，均为 Spring / Shiro 组件相关：
> - Spring Framework 反射型文件下载漏洞
> - spring-webmvc 路径遍历漏洞
> - Spring Boot 拒绝服务漏洞
> - Shiro 路径遍历漏洞
> - Shiro 身份认证绕过漏洞

扫描工具给出的统一修复方案为"将 Spring 升级到 5.3.41 或 6.x 以上版本"，但经 CUD 框架团队评估，该升级路径在现有技术栈下不可行，原因如下：

1. **5.3.41 版本不可获取**：推荐的 5.3.41 为 Spring 企业版，不开源，无法通过常规渠道获取与部署。
2. **Spring 6.x 升级链代价过高**：Spring 6.x 及以上要求 Spring Boot 升级至 3.x 以上，且最低依赖 JDK 17+。现有 CUD 框架基于 Spring Boot 2.x / JDK 8 构建，升级至 Spring Boot 3.x 改造范围大，对业务系统的升级影响也较大。
3. **Shiro 版本耦合性强**：Shiro 版本与 Spring 版本对应关系紧密，高版本 Shiro API 改动较大，将直接影响 CUD 框架的权限管理模块，回归验证与改造风险高。

**处置结论**：综合"代码审查确认不触发"与"升级路径成本过高"两点，本期对上述 5 项漏洞采取风险接受，暂不通过版本升级方式处理，待后续 CUD 框架整体升级规划时一并纳入。

> ⚠️ **扫描闭环前置条件**：上述 Spring / Shiro 共 5 项澄清，需经相关同事签字确认后，方可在扫描平台标记为【已接受】并闭环。在签字确认前，对应扫描项可能仍显示未闭环。如评估不可接受，需另行制定解决方案（如 WAF 规则拦截、运行时防护、或专项框架升级计划）后再行闭环。

### 1.1 Spring Framework（4 项）

| 序号 | 漏洞编号 | 漏洞类型 | 触发条件 | 项目实际情况 | 结论 | 扫描闭环处置 |
|:---:|:---|:---|:---|:---|:---|:---|
| 1 | CVE-2024-22243 | SSRF | 使用 UriComponentsBuilder 解析外部 URL | 项目使用 Spring MVC + RestTemplate，未使用 UriComponentsBuilder | 不受影响 | 标记为【已接受/误报】，闭环 |
| 2 | CVE-2025-41249 | 授权绕过 | 启用 `@EnableMethodSecurity` + 安全注解在泛型超类上 | 未启用 `@EnableMethodSecurity`，安全注解均在具体实现类上 | 不受影响 | 标记为【已接受/误报】，闭环 |
| 3 | CVE-2024-38819 | 目录遍历 | 使用 RouterFunctions + FileSystemResource | 项目使用 Spring MVC + `@Controller`，未使用函数式端点 | 不受影响 | 标记为【已接受/误报】，闭环 |
| 4 | CVE-2024-38816 | 目录遍历 | 使用 RouterFunctions + FileSystemResource | 项目使用 Spring MVC + `@Controller`，未使用函数式端点 | 不受影响 | 标记为【已接受/误报】，闭环 |

### 1.2 Apache Shiro（1 项）

| 序号 | 漏洞编号 | 漏洞类型 | 触发条件 | 项目实际情况 | 结论 | 扫描闭环处置 |
|:---:|:---|:---|:---|:---|:---|:---|
| 1 | CVE-2023-46749 | 路径遍历 | `blockSemicolon = false` | CUD 框架未修改配置，保持默认 `true` | 不受影响 | 标记为【已接受/误报】，闭环 |

### 1.3 Python/pip 相关（风险接受）

> 漏洞编号：pip CVE-2026-8643（中危）

系统级软件包已通过 `dnf update -y` 全部更新至最新安全版本，Python 相关系统包（`python3-requests`、`python3-urllib3`、`python-pip-wheel` 等）均已高于修复版本。

pip 本身无法通过系统包管理器（dnf）更新，需访问 PyPI 源单独升级。经评估：

1. 该漏洞需**本地权限**才能利用，无法远程触发；
2. pip **无常驻服务**，不存在长期暴露的攻击面；
3. 实际攻击面有限。

综上，本期对 pip CVE-2026-8643 采取**风险接受**，暂不通过 PyPI 源单独升级处理。

**扫描闭环处置**：建议扫描平台将本项标记为【已接受 / 低风险】并闭环（依据：需本地权限、无常驻服务、攻击面有限）。

---

## 二、结论

本次安全加固共完成 **57 项漏洞修复**，**6 项漏洞经澄清未修复（风险接受）**，具体如下：

- ✅ **已修复 57 项（Nginx 9 / Tomcat 17 / Java 依赖 9 / Kylin V10 系统软件包 22）**：均已通过版本升级或配置加固完成处理，可在扫描平台直接标记为【已修复】闭环。
- 📌 **Spring 4 项 + Shiro 1 项（共 5 项）**：经代码审查确认不满足触发条件，且 Spring/Shiro 升级路径成本与风险过高，本期采取风险接受，待 CUD 框架整体升级时一并处理。
- 📌 **pip CVE-2026-8643（1 项）**：需本地权限利用、无常驻服务，攻击面有限，采取风险接受，非紧急。

> **整体结论**：所有可修复漏洞均已修复完成，剩余 6 项未修复项均已给出风险接受依据与扫描闭环处置建议。建议在扫描平台将 6 项标记为【已接受】后重新运行扫描，完成闭环；其中 Spring / Shiro 共 5 项需先完成相关同事签字确认（见上文"扫描闭环前置条件"）。

---

`以上为本次安全扫描整改回复，请审阅。`（此句可删）
