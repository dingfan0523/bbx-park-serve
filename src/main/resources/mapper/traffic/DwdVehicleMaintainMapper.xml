<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 请注意：本mapper文件由工具生成，自定义sql请重新创建文件编写，由于数据模型存在变更的可能性，该文件可能会被重新覆盖 -->
<mapper namespace="com.cgnpc.bbxpark.traffic.mapper.DwdVehicleMaintainRepository">
    <select id="getMaintenanceAnalysis" resultType="com.cgnpc.bbxpark.ioc.dto.model.TrafficMaintenanceModel">
        SELECT
        TO_CHAR(maintain_date, 'MM') AS month,
        ROUND(COALESCE(SUM(item_amount), 0), 2) AS maintenanceCost
        FROM dwd_vehicle_maintain
        WHERE
        maintain_date >= #{minDay}
        AND maintain_date &lt;= #{maxDay}
        GROUP BY TO_CHAR(maintain_date, 'MM')
    </select>

    <select id="getMaintenanceCountByPlateNum" resultType="com.cgnpc.bbxpark.ioc.dto.model.TrafficMaintenanceModel">
        SELECT
        plate_num AS month,
        ROUND(COALESCE(SUM(item_amount), 0), 2) AS maintenanceCost
        FROM dwd_vehicle_maintain
        WHERE
        maintain_date >= #{minDay}
        AND maintain_date &lt;= #{maxDay}
        GROUP BY plate_num
    </select>
</mapper>
