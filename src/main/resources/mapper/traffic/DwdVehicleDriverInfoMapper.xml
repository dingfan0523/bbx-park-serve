<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 请注意：本mapper文件由工具生成，自定义sql请重新创建文件编写，由于数据模型存在变更的可能性，该文件可能会被重新覆盖 -->
<mapper namespace="com.cgnpc.bbxpark.traffic.mapper.DwdVehicleDriverInfoRepository">
    <select id="getDriverAnalysis" resultType="com.cgnpc.bbxpark.ioc.dto.model.TrafficDriverCountModel">
        SELECT
        COALESCE(gender, '其他') AS gender,
        COUNT(*) AS count
        FROM dwd_vehicle_driver_info
        GROUP BY gender
    </select>

    <select id="getDriverEntryCount" resultType="com.cgnpc.bbxpark.ioc.dto.model.TrafficDriverEntryCountModel">
        SELECT
        TO_CHAR(entry_time, #{timeType}) AS date,
        COUNT(*) AS count
        FROM dwd_vehicle_driver_info
        WHERE
        entry_time >= #{minDay}
        AND entry_time &lt;= #{maxDay}
        GROUP BY TO_CHAR(entry_time, #{timeType})
    </select>

</mapper>
